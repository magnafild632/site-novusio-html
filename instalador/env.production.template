# 🔧 Configuração de Produção - Site Novusio
# Template para arquivo .env em produção

# Configurações do servidor
NODE_ENV=production
PORT=3000

# Configurações do domínio
DOMAIN=your-domain.com
EMAIL=seu-email@exemplo.com

# Configurações de autenticação JWT
JWT_SECRET=sua-chave-secreta-muito-forte-aqui-minimo-32-caracteres
JWT_EXPIRES_IN=1h

# Configurações do banco de dados
DB_PATH=/opt/novusio/app/database.sqlite

# Configurações de upload
MAX_FILE_SIZE=52428800
UPLOAD_PATH=/opt/novusio/app/client/uploads

# Configurações de email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app

# Configurações de segurança
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configurações de logs
LOG_LEVEL=info
LOG_FILE=/var/log/novusio/app.log

# Configurações de backup
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# Configurações de monitoramento
HEALTH_CHECK_ENABLED=true
METRICS_ENABLED=true

# Configurações de cache
CACHE_ENABLED=true
CACHE_TTL=3600

# Configurações de CORS
CORS_ORIGIN=https://your-domain.com
CORS_CREDENTIALS=true

# Configurações de sessão
SESSION_SECRET=sua-chave-de-sessao-muito-segura
SESSION_MAX_AGE=86400000

# Configurações de rate limiting
RATE_LIMIT_API=100
RATE_LIMIT_LOGIN=5
RATE_LIMIT_ADMIN=20

# Configurações de SSL
SSL_ENABLED=true
SSL_REDIRECT=true

# Configurações de backup automático
AUTO_BACKUP_ENABLED=true
AUTO_BACKUP_TIME=02:00
AUTO_BACKUP_RETENTION=7

# Configurações de notificações
NOTIFICATIONS_ENABLED=true
NOTIFICATION_EMAIL=admin@your-domain.com

# Configurações de manutenção
MAINTENANCE_MODE=false
MAINTENANCE_MESSAGE=Site em manutenção. Volte em breve.

# Configurações de debug (manter false em produção)
DEBUG=false
VERBOSE_LOGGING=false

# Configurações de performance
CLUSTER_MODE=false
WORKER_PROCESSES=1
MAX_MEMORY=1073741824

# Configurações de timeout
REQUEST_TIMEOUT=30000
CONNECTION_TIMEOUT=5000

# Configurações de compressão
COMPRESSION_ENABLED=true
COMPRESSION_LEVEL=6

# Configurações de segurança adicional
SECURITY_HEADERS=true
XSS_PROTECTION=true
CSRF_PROTECTION=true

# Configurações de banco de dados (para futuras migrações)
DB_TYPE=sqlite
DB_HOST=localhost
DB_PORT=5432
DB_NAME=novusio
DB_USER=novusio
DB_PASS=senha-segura-do-banco

# Configurações de Redis (opcional, para cache)
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASS=

# Configurações de CDN (opcional)
CDN_ENABLED=false
CDN_URL=https://cdn.your-domain.com

# Configurações de analytics (opcional)
ANALYTICS_ENABLED=false
GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX

# Configurações de SEO
SEO_ENABLED=true
SITEMAP_ENABLED=true
ROBOTS_TXT_ENABLED=true

# Configurações de API
API_VERSION=v1
API_RATE_LIMIT=1000
API_TIMEOUT=30000

# Configurações de upload de arquivos
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,txt
MAX_FILES_PER_REQUEST=10
UPLOAD_TEMP_DIR=/tmp/novusio-uploads

# Configurações de validação
VALIDATION_STRICT=true
SANITIZE_INPUT=true

# Configurações de cache de arquivos estáticos
STATIC_CACHE_TTL=31536000
STATIC_CACHE_ENABLED=true

# Configurações de monitoramento de performance
PERFORMANCE_MONITORING=true
SLOW_QUERY_THRESHOLD=1000

# Configurações de backup de banco
DB_BACKUP_ENABLED=true
DB_BACKUP_SCHEDULE=0 3 * * *
DB_BACKUP_RETENTION=30

# Configurações de logs de auditoria
AUDIT_LOGS_ENABLED=true
AUDIT_LOG_RETENTION=90

# Configurações de notificações de erro
ERROR_NOTIFICATIONS_ENABLED=true
ERROR_NOTIFICATION_EMAIL=errors@your-domain.com

# Configurações de manutenção programada
SCHEDULED_MAINTENANCE_ENABLED=false
SCHEDULED_MAINTENANCE_START=02:00
SCHEDULED_MAINTENANCE_DURATION=60

# Configurações de backup em nuvem (opcional)
CLOUD_BACKUP_ENABLED=false
CLOUD_BACKUP_PROVIDER=aws
CLOUD_BACKUP_BUCKET=novusio-backups

# Configurações de monitoramento externo
EXTERNAL_MONITORING_ENABLED=false
MONITORING_URL=https://status.your-domain.com

# Configurações de desenvolvimento (manter false em produção)
DEV_MODE=false
HOT_RELOAD=false
SOURCE_MAPS=false
